server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: git@gitee.com:radebit/rade-cloud-config.git
          search-paths:
            - rade-cloud-config
        default-label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
